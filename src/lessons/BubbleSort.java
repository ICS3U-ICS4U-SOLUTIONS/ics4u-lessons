package lessons;

public class BubbleSort {

	/*
	 
	BUBBLE SORT (ascending)

	// variables
	boolean swapped = false;

	STEP 1
	Compare first element with second element.  If first element is larger than second element, swap.
	Set BOOLEAN named "swapped" to TRUE.

	STEP 2
	Repeat until first element is array.length - 2.

	STEP 3
	Set swapped = false;

	STEP 4
	Repeat Steps 1-3 until swapped is false.
	
	*/
}
